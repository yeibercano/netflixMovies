/* eslint-env mocha */ 

const { expect } = require('chai')

describe('<Search />', () => {
  it('should pass', () => {
    expect(1 + 1 === 2).to.be.true
  })
  it('should equal', () => {
    expect(3+ 2).to.equal(5)
  })
})
